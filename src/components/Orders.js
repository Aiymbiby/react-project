import React from "react";

const Orders = () => {
  return (
    <div className="page">
      <h2>My Orders</h2>
      <p>No orders yet (demo page for routing requirement)</p>
    </div>
  );
};

export default Orders;